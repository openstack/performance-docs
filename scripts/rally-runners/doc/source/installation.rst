============
Installation
============

At the command line::

    $ pip install rally-runners

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv rally-runners
    $ pip install rally-runners
